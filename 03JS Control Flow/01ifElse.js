let x = 5;
if (x > 10) {
        console.log('X is greater than 10');
} else {
        console.log('X is smaller than 10');
}

// conditional Operators Or Ternary Operator
console.log((10 > 20) ? 'Greater' : 'Smaller');


let age = 19;
console.log((age > 18) ? 'yes' : 'no');

var locked = 2;
var canChange = locked != 1 ? true : false;
console.log(canChange);