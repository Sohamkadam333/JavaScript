 let x = a[propName];
                let y = b[propName];


                if (x > y) {
                        return 1;
                } else if (x < y) {
                        return -1;
                } else {
                        return 0;
                }