const btn = ['primary', 'seconday', 'danger', 'success'];
console.log(btn.join('+'));

// join method converts an arrya into string and join it by parameter passed in string


const str = 'Javascript is a Funny Language';
console.log(str.split(' ').join('-').toLowerCase());
